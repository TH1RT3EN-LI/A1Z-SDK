# Optional GPU-host overrides. Copy to the ignored
# config/remote_gpu_server.env only when the defaults need adjustment.
A1Z_REMOTE_GPU_LOCK_FILE=/tmp/a1z-gpu0.lock
A1Z_REMOTE_GPU_TARGET_TIMEOUT_S=360
A1Z_REMOTE_GPU_ANYGRASP_TIMEOUT_S=360
A1Z_REMOTE_GPU_STALE_JOB_AGE_S=86400
